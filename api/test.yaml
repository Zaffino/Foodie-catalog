// file di test per eventuali errori di indentazione per le stringhe per Swagger

/api/menu:
 *  get:
 *      summary: Estrazione di tutti i menu
 *      description: Estrazione di una lista di menu dal file menu.json sul server in locale 
 *      parameters:
 *        - in: query
 *          name: test
 *          schema:
 *              type:string
 *          required: false
 *          description: Deve essere = true quando si sta eseguendo un test con tape.
 *      responses:
 *          200:
 *              description: Una lista di menu
 *              content:
 *                  application/json:
 *                      schema:
 *                          type: object
 *                          properties:
 *                              data:
 *                                  type: array
 *                                  items:
 *                                      type: object
 *                                      properties:
 *                                          menuID:
 *                                              type: integer
 *                                              description: Il numero identificativo del menu
 *                                              example: 1
 *                                          menuType:
 *                                              type: string
 *                                              description: La tipologia di menu, scelta tra "table" e "take away"
 *                                              example: "take away"
 *                                          menuList:
 *                                              type: object Array
 *                                              description: La lista dei piatti contenuti nel menu
 *                                              example : example text