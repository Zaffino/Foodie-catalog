var Express = require ("express");
var app = Express ();
var fs = require ("fs");
var cors = require('cors');
app.use(cors());

var serverboot = document.getElementById('root');
//module.exports = 'express';